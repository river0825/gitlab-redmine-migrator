// import { GitlabIssue } from "../Issues/Issue";

export interface IssueQuery{
    label: string[];
}

export interface GitlabRepo
{
    /**
     * list issues from Repo
     * @param query 
     * @param arg1 
     */
    // issues(query: IssueQuery, arg1: (issue: GitlabIssue) => void): void;
}
